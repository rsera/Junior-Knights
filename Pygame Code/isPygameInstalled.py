# This is a short program to test if pygame is installed
import pygame

pygame.init()

# This creates a window that is 640 by 480 pixels
window = pygame.display.set_mode((640, 480))

# This draws the window
pygame.display.update()

